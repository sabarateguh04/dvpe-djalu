{
  "name": "dvpe-mockup",
  "version": "0.1.0",
  "private": true,
  "description": "DVPE (Digital Victim Protection Ecosystem) - initial clickable mockup: Node/Express API + Vite dashboard/portal (single port 4002) + Flutter mobile client.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -k -n WEB,API -c blue,green \"npm run dev --workspace=web\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=web",
    "start": "npm run build && npm run start --workspace=server",
    "start:nobuild": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18.18"
  }
}
