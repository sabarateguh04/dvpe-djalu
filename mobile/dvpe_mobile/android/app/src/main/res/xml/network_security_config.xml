<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ blocks plaintext HTTP by default. This mockup's backend runs
  over plain HTTP on localhost during development (see ../../../../server),
  so cleartext is allowed ONLY for the loopback/emulator-host addresses used
  to reach it locally.

  Before shipping this app against a real/deployed backend, remove this
  cleartext allowance entirely and point apiBaseUrl (lib/config.dart) at an
  https:// URL - the base-config block below (cleartextTrafficPermitted
  false) already enforces HTTPS for every other host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
